name: flutter_ffi_uvc
description:  Connect USB (UVC) cameras to Flutter — live Texture preview, native JPEG capture, raw frame access, and camera controls. No extra drivers.
version: 0.9.0
repository: https://github.com/cornpip/flutter_ffi_uvc

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

topics:
  - camera
  - usb
  - uvc
  - webcam
  - libuvc

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.3

dev_dependencies:
  ffigen: ^20.1.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.cornpip.flutter_ffi_uvc
        pluginClass: FlutterFfiUvcPlugin
        ffiPlugin: true
      windows:
        pluginClass: FlutterFfiUvcPluginCApi
