# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterFfiUvcBindings
description: |
  Bindings for `src/include/flutter_ffi_uvc.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/src/flutter_ffi_uvc_bindings_generated.dart'
headers:
  entry-points:
    - 'src/include/flutter_ffi_uvc.h'
  include-directives:
    - 'src/include/flutter_ffi_uvc.h'
comments:
  style: any
  length: full
